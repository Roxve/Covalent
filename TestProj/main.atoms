# test project 
# TODO: added assert instead of writeln

set x: a { 
	ret a 
} 

# TODO argless functions using !
set do_stuff: temp {
	set z = 25
	set y = z
	
	set a = x: 5.0
	set b = a
	writeln: b * 5

	writeln: 6 > 5
	writeln: 5 < 6
	writeln: (x: 25) >= (x: 25.0) 

	writeln: 7 <= 8

	writeln: a * 5 == 25
	writeln: "Hi" == "Hi"
	a = x: "long"
	b = x: "short"
	writeln: a > b # strings compare operations is only dynamic for now
	writeln: "Hello, " + "world!"
	writeln: temp
}
do_stuff: "None"

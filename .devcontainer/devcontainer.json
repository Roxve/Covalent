{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
  }, 
  "onCreateCommand": "sudo apt update -y && sudo apt upgrade -y",
  "onCreateCommand": "sudo add-apt-repository ppa:maveonair/helix-editor -y && sudo apt update -y && sudo apt install helix fish zoxide -y",
  "onCreateCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh",
  "onCreateCommand": "rustup default stable",
  "onCreateCommand": "git clone --depth 1 https://github.com/crivotz/nv-ide.git ~/.nv-ide && cd ~/.config && ln -s ~/.nv-ide nvim" 
}

set num >> 0;

set createUser(name, pass) : {
  num >> num + 1;
  return { name, pass,num };
}

set user0: createUser("ahmed", 777);

set user1: createUser("thomas", 666);


write(user0.name, " ", user0.pass);
write(user1.name, " ", user1.pass);
write("all nums: ", user0.num, ",", user1.num, ",")
